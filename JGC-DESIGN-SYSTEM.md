# JGC Portal Design System

The design system is the shared visual source for new portal pages and for existing sections that are deliberately migrated. It is loaded by `common.js`, so every portal page receives the same tokens and components.

## Compatibility Rule

Do not replace an existing page's table, form, card, or feature CSS simply to adopt the design system. Existing page styles remain intact until that section is tested and intentionally migrated.

All shared design-system classes begin with `jgc-`. Generic names such as `.card`, `.panel`, `.tabs`, `.button`, and `table` must not be added to the shared stylesheet because they can change older pages unexpectedly.

## New Page Shell

```html
<body class="jgc-page">
  <main class="jgc-page-shell">
    <header class="jgc-page-header">
      <div class="jgc-brand"><img src="logo.webp" alt="John Gordon Construction"></div>
      <h1>Page Name</h1>
      <p>Signed in as: Employee Name</p>
    </header>

    <section class="jgc-panel">
      <h2 class="jgc-section-title">Section Name</h2>
    </section>
  </main>
</body>
```

Use `jgc-page-shell--wide` for admin pages or dense reporting views.

## Page Logo

The primary logo at the top of a portal page uses `--jgc-page-brand-width` and is standardized at 347px, with responsive side gutters on narrow screens. Use the `jgc-brand` wrapper shown above, or the shared admin header hook, so new pages inherit this size automatically.

Compact app-bar branding on Home and the bespoke Login branding are intentional exceptions. Logos inside PDFs, QR print layouts, inspection sheets, and modal content are not governed by the primary page-logo rule.

## Interaction Standards

Every focusable control inside a `jgc-page` receives the shared high-visibility focus ring when it is reached with a keyboard. Pages must not remove this focus state. The design system also respects a device's reduced-motion preference by suppressing nonessential transitions and animations.

## Buttons

```html
<button class="jgc-button" type="button">Save</button>
<button class="jgc-button jgc-button--secondary" type="button">Close</button>
<button class="jgc-button jgc-button--danger" type="button">Cancel</button>
<button class="jgc-button jgc-button--icon" type="button" aria-label="Refresh" title="Refresh">
  <i data-lucide="refresh-cw"></i>
</button>
```

Use icon-only buttons for familiar commands such as close, refresh, download, and delete. Always include `aria-label` and `title`.

## Forms

```html
<div class="jgc-form-grid">
  <div class="jgc-field">
    <label class="jgc-label" for="job">Job</label>
    <select class="jgc-select" id="job"></select>
  </div>
  <div class="jgc-field">
    <label class="jgc-label" for="date">Date</label>
    <input class="jgc-input" id="date" type="date">
  </div>
  <div class="jgc-field jgc-field--full">
    <label class="jgc-label" for="notes">Notes</label>
    <textarea class="jgc-textarea" id="notes"></textarea>
  </div>
</div>
```

## Tables

```html
<div class="jgc-table-wrap">
  <table class="jgc-table">
    <thead>
      <tr><th>Name</th><th>Status</th><th>Actions</th></tr>
    </thead>
    <tbody>
      <tr>
        <td>Example</td>
        <td><span class="jgc-badge jgc-badge--success">Active</span></td>
        <td><div class="jgc-table-actions"></div></td>
      </tr>
    </tbody>
  </table>
</div>
```

Existing tables do not need these classes unless they are being migrated and visually checked on desktop and mobile.

## Tabs And Feedback

Use `jgc-tabs` as the container and `jgc-tab` for each tab. Use `active`, `aria-selected="true"`, or `aria-current="page"` for the selected state.

Feedback components are `jgc-notice`, `jgc-notice--warning`, and `jgc-notice--danger`. Status labels use `jgc-badge` with the success, warning, danger, or info modifier.

## Shared Navigation

The top portal navigation and mobile sticky navigation are generated by `common.js`. Do not duplicate either menu inside a new page.

The mobile sticky navigation keeps Home, Timesheets, PO, WO, and Inspections as quick links, followed by a More button that opens the remaining portal destinations.

The admin navigation is also generated from the single `JGC_ADMIN_NAV_ITEMS` definition in `common.js`. Admin pages may retain their original menu markup as a no-JavaScript fallback, but labels, order, destinations, active state, and accessibility are replaced from the shared definition at runtime.

Pages named `*-admin.html`, plus `admin.html`, `accounts.html`, and `notification-settings.html`, receive the shared admin navigation automatically. A new admin page does not need a copied list of admin buttons.

The shared hook adds `jgc-admin-page-title`, `jgc-admin-user-line`, and `jgc-admin-brand-region` to the existing admin header elements. These are compatibility hooks: feature-specific page layout remains in the page stylesheet. Feature-level tabs are never treated as admin navigation.

## Migration Status

- **Step 1:** shared tokens and opt-in components, loaded on every portal page.
- **Step 2:** centralized admin navigation and semantic admin header hooks.
- **Step 3:** controlled page migration covers the employee and admin Purchase Orders pages. Existing `po-*` hooks remain in place while shared page, button, tab, form, badge, notice, section, action, card, table, and modal classes are added alongside them.
- **Step 4:** the Accounts page now uses shared page, admin navigation, header, panel, filter, input, button, badge, table, and action semantics. Its existing light permissions panel, status colors, dimensions, and workflows remain protected by page-specific compatibility rules.
- **Step 5:** the standalone Work Orders page and the Admin Work Orders section now use shared page, header, panel, section, filter, form, input, button, tab, badge, table, and action semantics. Existing WO layouts, mobile table scrolling, embedded editor behavior, status colors, and submission workflows remain protected by feature-specific compatibility rules.
- **Step 6:** the Inspections landing page uses shared components directly; Today's Inspections and Previous Inspections now use `jgc-design-system.css` plus token-only page layout stylesheets, while the Admin Safety Records tiles and detailed inspection sheets use the scoped token-only `safety-records-admin.css` extension. Legacy page styling, inline screen-layout rules, and Admin inspection rules in `admin.css` have been removed. Generated inspection-report styling remains isolated for print/PDF output. Existing inspection cards, history-table geometry, mobile scrolling, report editing, acknowledgement controls, detailed inspection sheets, PDF output, and inspection workflows remain protected.
- **Step 7:** the employee Certificates page uses the shared system directly, standalone Admin Certificates uses one token-only `certificates-admin.css` layout source, and the embedded Admin Certificates tool uses a scoped token-only `certificates-embedded.css` extension. Legacy `styles.css`, embedded styling, Admin certificate rules in `admin.css`, and runtime spacing writes have been removed. Existing expiry alerts, upload controls, training-matrix sticky columns, signed file links, record grouping, deletion, and mobile table scrolling remain protected.
- **Step 8:** the employee Vacation Request page and Admin Vacation Requests section now use shared page, header, panel, section, form, filter, input, button, badge, notice, empty-state, table, toolbar, card, and action semantics. Existing request-form geometry, summary tiles, vacation calendars, status colours, admin review notes, approval and denial controls, email workflow, deletion behavior, and mobile table scrolling remain protected by feature-specific compatibility rules.
- **Step 9:** the employee Equipment / Vehicles directory uses the shared system directly, and the embedded Admin Equipment / Vehicles tool now uses `jgc-design-system.css` plus one token-only `equipment-admin.css` layout source. Equipment-specific rules have been removed from `admin.css`; runtime colour and spacing writes have been replaced with shared status and layout classes. Existing equipment grouping, kilometre and hour meters, expiry notifications, maintenance logs, QR generation and printing, lift-document uploads, signed PDF access, edit and delete workflows, and mobile table scrolling remain protected.
- **Step 10:** the Permits directory, Hot Work Permit, Confined Space Entry Permit, and Excavation Permit now use `jgc-design-system.css` plus one token-only `permit-design-system.css` layout source. Legacy stylesheets, embedded CSS, and JavaScript colour writes have been removed; readiness banners use shared success/danger states. Existing checklists, project/job selectors, authorization fields, inspection-record saves, offline behavior, print layouts, Today/Previous navigation, and mobile single-column forms remain protected.
- **Step 11:** the Reports directory uses shared components directly; Daily Site Report, JSA, Near Miss / Incident Report, Supervisor Accident Investigation, Employee Injury Report, and Toolbox Talks now use `jgc-design-system.css`, the token-only shared `report-design-system.css`, and one token-only form-layout stylesheet apiece. Embedded Admin Reports use the scoped token-only `reports-admin.css` extension, and report-specific rules have been removed from `admin.css`. Legacy screen stylesheets, page-owned screen style blocks, and inline screen-layout rules have been removed; generated PDF/print document styling remains isolated. Existing report fields, photos, PDF and email generation, employee selectors, crew sign-off, toolbox PDF access, acknowledgements, inspection-record saving, admin report subtabs, offline behavior, print layouts, and mobile single-column forms remain protected.
- **Step 12:** the employee Timesheet page and Admin Timesheets section now use the shared portal foundation and a single token-only timesheet-family stylesheet as their complete visual source. Employee page CSS and Admin Timesheet record-group styling no longer live in page markup or `admin.css`. Existing calendar navigation, active-job search, WO requested-hour prefills, work and leave entries, weekly summaries, mobile card/table views, offline synchronization, weekly PDF and email submission, admin-added time, employee groupings, submitted-week viewing and editing, resubmission, deletion workflows, and generated PDF styling remain protected.
- **Step 13:** the employee Tasks page and embedded Admin Tasks section now use a shared tasks-family compatibility stylesheet plus shared page, brand, panel, section, form, field, input, button, notice, tabs, filter, card, and action semantics. Existing task assignment, job links, due dates, priorities, categories, status changes, editing, deletion, embedded admin resizing, and mobile task-card behavior remain protected.
- **Step 14:** the employee Policies / Announcements page, standalone Admin Policies page, and embedded Admin Announcements / Policies section now use shared page, brand, panel, section, form, field, input, button, notice, table, card, and action components as their complete visual source. No policy-specific or page-owned CSS remains. Existing lazy loading, acknowledgement tracking, PDF links, uploads, admin CRUD, email delivery, and mobile table scrolling remain protected.
- **Step 15:** the employee Contacts and Subcontractors / Suppliers directories and their embedded Admin editors now use `jgc-design-system.css` plus one token-only `directory-design-system.css` layout source. Standalone legacy and inline styling has been removed; Admin forms, tables, company panels, contact cards, actions, and notices use shared components. Existing Supabase lazy loading, CRUD, search, telephone/email links, and company/contact selection workflows remain protected.
- **Step 16:** the employee Job Lookup page, Admin Job Dashboard / Job Costing section, and Admin Jobs Management section now use a shared jobs-family compatibility stylesheet plus shared page, brand, panel, section, field, input, select, button, table, and action semantics. Existing active-job lookup, document links, Excel import rules, red-row cancellation handling, active and inactive states, dashboard calculations, and mobile table scrolling remain protected.
- **Step 17:** the employee Schedule page and Admin Summary Schedule Calendar now use a shared schedule-family compatibility stylesheet plus shared page, brand, panel, section, input, select, button, modal-dialog, and action semantics. The legacy Admin schedule backdrop remains controlled by its existing `open` state. Existing work, equipment and vehicle, training, general, and vacation calendar items, employee tagging, Google synchronization, email notices, editing, deletion, calendar navigation, and mobile single-column forms remain protected.
- **Step 18:** the Aerial Lift, Forklift, Harness, and Telehandler specialty inspection checklists now use `jgc-design-system.css` plus one token-only `specialty-inspection-design-system.css` layout source. Legacy stylesheets and JavaScript colour writes have been removed; desktop tables and synchronized mobile checklist cards share the same form, status, table, action, focus, and theme tokens. Existing inspection questions, equipment selectors, project and job entry, pass/fail/not-applicable controls, acknowledgements, offline records, save workflows, and print layouts remain protected.
- **Step 19:** the QR Vehicle and Equipment inspection pages now load `jgc-design-system.css`, the shared token-only `qr-inspection-design-system.css` layout layer, and one token-only page layout stylesheet apiece. Page-owned style blocks, inline screen-layout rules, and local colour palettes have been removed; generated print-document styling remains intentionally isolated from the portal screen theme. Existing QR token handling, manual vehicle selection, current-day inspection views, lift manuals, yearly inspection reports, dynamic checklists, pass/defect/not-applicable controls, draft and submission workflows, history, and print/PDF output remain protected.
- **Step 20:** the employee Home dashboard now uses a token-only Home compatibility stylesheet plus shared page, brand, header, panel, card, section-title, action, form, notice, and modal-dialog semantics. Its former inline and legacy stylesheet rules and duplicate mobile menu were removed; the shared navigation is now the only mobile navigation. Existing profile controls, quick information, vacation reminders, overview statistics, announcements, schedule agenda, quick-access cards, sidebar behavior, mobile layout, and all dashboard data workflows remain protected.
- **Step 21:** the full-screen Field Calculator now uses `jgc-design-system.css` plus one token-only `field-calculator.css` layout source. Its duplicate compatibility layer and page-owned colour values have been removed; the physical keypad, display, quick calculators, tape, preferences, and guide inherit centralized theme tokens while retaining purpose-built field-tool geometry. Existing construction-math functions, Sonotube and stair calculators, safe-area handling, compact-height behavior, and mobile full-screen layouts remain protected.
- **Step 22:** the standalone Admin Notification Settings page now uses centralized admin navigation, `jgc-design-system.css`, and one token-only notification-settings layout source. Its page-owned style block, local palette, and legacy `styles.css` dependency have been removed. Shared page, brand, panel, section-title, toolbar, form, field, input, button, table, and action semantics protect notification-type creation, employee/supervisor/admin delivery toggles, Supabase loading and saving, status feedback, wide-table scrolling, and mobile single-column layout.
- **Step 23:** the public Safety Acknowledgement page now uses a scoped acknowledgement compatibility stylesheet plus shared page, brand, panel, card, label, input, select, textarea, and button semantics. Existing QR-token validation, JSA and Toolbox Talk record loading, attendee selection, unmatched attendee entry, confirmation requirements, public Supabase RPC submission, status feedback, and mobile single-column layout remain protected.
- **Step 24:** the Reset Password page now uses a scoped reset-password compatibility stylesheet plus shared page, shell, brand, panel, field, label, input, button, and notice semantics. Existing Supabase recovery-session password updates, success feedback, delayed return to login, compact card geometry, and navigation exclusions remain protected; its stale deleted logo reference now uses the current cached WebP asset.
- **Step 25:** the Login page now uses a scoped login compatibility stylesheet plus shared page, shell, header, brand, panel, card, section, label, input, textarea, button, notice, action, and modal-dialog semantics. Existing employee authentication, account creation, password reset, stay-signed-in behavior, Field Calculator and Notes access, subcontractor entry, local scratch-pad storage, QR sharing, responsive hero layout, and navigation exclusions remain protected; both stale deleted logo references now use the current cached WebP asset.
- **Step 26:** the standalone Subcontractor Portal now uses a scoped subcontractor compatibility stylesheet plus shared page, shell, header, brand, panel, card, button, and action semantics. Existing subcontractor session checks, Reports, Inspections, Permits, Policies, and Contacts destinations, sign-out behavior, responsive card grid, and mobile layout remain protected; its stale deleted logo reference now uses the current cached WebP asset.
- **Step 27:** shared interaction standards now provide a consistent high-visibility keyboard focus ring and reduced-motion support across all `jgc-page` portal screens. Existing controls, layouts, colours, table behavior, and workflows remain protected.
- **Step 28:** the employee Job Notes editor and standalone Job Notes Admin page now use `jgc-design-system.css` plus one token-only `job-lists.css` layout source. Legacy page styling has been removed; list folders, note rows, full-screen editing, people and reminders, autosave states, statistics, filters, tables, modals, and actions inherit the centralized theme and component tokens. Existing offline synchronization, note completion, employee tagging, reminder delivery, admin management, and deletion workflows remain protected.
- **Step 29:** the standalone Employee Page Access and Portal Diagnostics admin tools now load `jgc-design-system.css` directly plus one scoped token-only layout stylesheet apiece. Their legacy `styles.css` dependency and local colour palettes have been removed. Existing account/manual employee inclusion controls, Accounting visibility selection, feature dependencies, diagnostics summaries, filters, event details, resolution controls, admin navigation, mobile access-table scrolling, and mobile diagnostics cards remain protected.
- **Step 30:** the read-only Limited Access portal now uses `jgc-design-system.css` plus one token-only `limited-access.css` layout source. Its page-owned style block, local palette, and legacy `styles.css` dependency have been removed; panels, tabs, notices, and the page shell use shared semantics. Existing account-status routing, personal certificate/timesheet/inspection/report lazy loading, signed certificate access, record-detail modal, empty/error states, and mobile single-column records remain protected.
- Existing data tables, feature tabs, forms, cards, and workflows remain unchanged until deliberately migrated and tested.

## Change Checklist

1. Use an existing `jgc-*` component before creating new styling.
2. Add a new design token instead of copying a colour or spacing value across pages.
3. Keep feature-specific layouts in their feature stylesheet.
4. Check desktop and mobile widths before removing any legacy CSS.
5. Advance the service-worker cache when shared CSS or navigation changes.
