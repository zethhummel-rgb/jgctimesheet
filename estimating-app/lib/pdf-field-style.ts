import { rgb } from "pdf-lib";

// Neutral outlines remain visible without changing the existing pale fills.
export const PDF_FIELD_LINE = rgb(0.72, 0.72, 0.72);
export const PDF_FIELD_BORDER_WIDTH = 0.6;
